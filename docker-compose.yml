services:
  eva:
    container_name: eva
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - dokploy-network
    ports:
      - "3000:3000"
    restart: always

networks:
  dokploy-network:
    name: dokploy-network
    external: true
